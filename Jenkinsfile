pipeline{
	any agent
	stages{
		stage('1-clone repo'){
			steps{
				sh 'pwd'
			}
		}
		stage('2-merge pull request'){
			steps{
				sh 'whoami'
			}
		}
		stage('3-check pipeline sync'){
			steps{
				sh 'df -h'
			}
		}
	}
}